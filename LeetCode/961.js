var repeatedNTimes = function(A) {
    const N = A.length/2
    for (number of A) {
		let count = 0;
       for (otherNumber of A) {
           if (number === otherNumber) {
               count++ } }
		if (count === N) {
            return number
        }
    }
};
